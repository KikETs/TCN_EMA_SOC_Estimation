# Reference SOC Generation

The queued sensitivity audit uses the existing nominal `SOC_CC` column as the reference label and evaluates scenario perturbations around the recorded `SOC0_used`, `Qnet_removed`, and `Qnet_denom` columns. These are scenario diagnostics, not a claim of instrument-ground-truth uncertainty.
