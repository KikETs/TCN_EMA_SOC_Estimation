# Source Metrics

This folder contains small summary files used to regenerate paper tables and figures.

It intentionally excludes:

- raw trajectories
- processed feature caches
- model checkpoints
- full prediction dumps

Run:

```bash
python scripts/build_paper_artifacts.py
```
